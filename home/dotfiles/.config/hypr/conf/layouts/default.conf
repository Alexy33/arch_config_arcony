# -----------------------------------------------------
# Layouts
# -----------------------------------------------------

dwindle {
    # pseudotile removed from dwindle config in Hyprland 0.55 — now per-window.
    # Use the `pseudo` dispatcher (e.g. bind = $mainMod, P, pseudo) or `windowrule = pseudo, <match>`.
    preserve_split = true
}

master {
    # Commented out due to compatibility reasons
    # new_status = master
}

binds {
  workspace_back_and_forth = true
  allow_workspace_cycles = true
  pass_mouse_when_bound = false
}
